# AFD 架构重设计（评审版）

**结论先说**：目标形态是**按层拆分**——每层一组注意力卡 + 一个加工单元（如 LPU），
层内用多卡分片 + continuous batching；**不做同卡多开 Attention 进程**。
全文按 §1.3 的硬限制来读。

> 术语：「**注意力工人**」指 Attention 侧，「**加工工人**」指 FFN 侧，
> 「**一趟传送**」指两侧之间搬一次数据。代码名词见文末附录。

---

## 一、一页看懂

### 1.1 问题

每一层都在做两件事，吃的资源完全不同：

|       | 注意力（Attention）            | 加工（FFN / 专家网络）      |
| ----- | ------------------------- | ------------------- |
| 吃什么资源 | **显存 + 访存带宽**（KV 随并发/上下文膨胀） | **算力**（专家权重）        |
| 怎么扩容  | 加卡、加带宽                    | 加算力（按层配加工单元）        |

绑在同一张卡上，两边的扩容诉求互相绑架：想要并发就得留显存放 KV，显存一紧挤掉权重；
想要算力就得留权重空间，又挤掉 KV。而且任一侧的突发都会拖慢另一侧。

**AFD 的主张**：拆成两个独立池子，中间只搬数据，两边各自按自己的瓶颈扩容。

### 1.2 三个方案的关系


| 方案               | 解决什么问题                             | 一句话机制                     | 现状              |
| ---------------- | ---------------------------------- | ------------------------- | --------------- |
| **一** 拆开         | 两种负载互相绑架扩容                         | 按**角色**拆（不是整机切层）          | **已完成**         |
| **二** 队列 + 通行证   | 两边速度不匹配、在飞状态不透明、活没处等、共用池多路来源时层号会混   | 等待区 / 在跑区 + 额度放行；加工侧按层分桶  | **已完成**，需重新定位用途 |
| **三** 按层拆分       | 进不了跨层流水线、同层合并只能运行时凑、层间互相等           | 每层独立注意力算力 + 每层独立加工单元      | **下一目标形态**      |

方案一是前提；拆开之后才出现「两边速度不匹配」，由方案二管。
但方案二只管节奏，**吞吐主线是方案三**；同卡多开注意力进程不是默认路径（理由见 §1.3）。

### 1.3 设计前提与硬限制

数字是量级假设，用来定瓶颈，不是承诺实测值。


| 项      | 取值（量级）                       | 对设计的含义                                             |
| ------ | ---------------------------- | -------------------------------------------------- |
| 模型     | GLM-5.2 量级（约 78 层 MoE + DSA） | 层数多 → 按层拆分后部署规模大                                   |
| 精度     | fp8                          | KV / 权重按字节计                                        |
| 目标吞吐   | **每层 5M TPS**                | token 流很大；但 seam 上的激活带宽并不大                         |
| 目标上下文  | **32768**                    | 每层每 token 读 KV ≈ **5 MB 量级**（访存主导）                 |
| 目标 TPOT | **约 1 ms**                   | 每层时间预算 ≈ `1ms / 层数`（十几微秒）；在飞 `C ≈ F×TPOT`，摊到每层就是小批 |
| 注意力侧   | **每层约 20 张 H800 级 GPU**      | 层内用 **TP 分片**，不是多进程副本                              |
| 加工侧    | **每层一个 LPU**                 | FFN 侧节奏确定；算力要按层配齐                                  |
| 组批方式   | **continuous batching**      | 有多少在飞算多少；组包与计算双缓冲重叠                                |

> **这组数字自己能对上**：每层 KV 读取 ≈ `5 MB × 5M TPS ≈ 25 TB/s`，与每层约 20 张 H800 的
> HBM 合计带宽同量级；在飞 `5M × 1ms = 5000` 个 token，摊到 78 层即每层每步几十个，
> 与「每层十几微秒」一致。**20 卡/层与 CB 小批是同一组约束的两面，不是两个独立假设。**

由此推出几条**硬判断**（后面方案都服从）：

1. **瓶颈在注意力侧 KV 访存**。每层 20 卡把带宽配齐后，同卡再开副本加不出 HBM。
2. **少等靠 CB + 双缓冲**，不靠多队列多副本：十几微秒的计算能盖住几微秒的组包。
3. **权重摊薄靠分片进 L2**，不靠复制、不靠切 microbatch（两者都让权重更难摊）。
4. **KV 不做「切完再全量 all-gather」式分片**：通信往往贵过本地 HBM 收益；优先按头 TP 或 KV 复制。
5. **方案三才是吞吐主线**；方案二只管节奏与背压。

---

## 二、方案一 · 按角色拆开

### 2.1 解决什么问题

- **扩容诉求互相绑架**：并发要显存放 KV，算力要显存放权重，互相挤。
- **两种性质相反的负载抢同一批资源**：一边访存密集、一边计算密集，突发互相拖累。

### 2.2 怎么解决

**按角色拆，不按「整机切层」。**
不是「前十层整机放 A、后十层整机放 B」——那样每台机器仍是注意力+加工绑在一起。
而是两侧各自持有全部层里属于自己的那部分：

```
 ══ 注意力工人 ═══════════════════════════════════════════════

    输入 → 注意力（全部层）→ 路由决策（全部层）→ 输出层 → 采样
                    │
                    └─ 历史缓存 ── 只在注意力侧
                       （方案一：集中在本工人；方案三：随各层卡组驻留）

                 │  传过去：中间数据 + 路由结果
                 ▼
                 ▲  传回来：加工结果

 ══ 加工工人 ═════════════════════════════════════════════════

    专家网络（全部层）
    · 没有注意力、没有历史缓存、没有输入输出层
    · 后台循环：取活 → 算 → 回结果，不直接对外服务
```

**为什么不能「只切层、不拆角色」**：每台仍要跑注意力并持有 KV，显存绑架等于没解决，
注意力权重也裁不掉。

> 这与方案三不冲突：方案三是**在角色已拆开的前提下**再按层铺硬件。
> 方案一禁止的是「不拆角色的整机切层」，不是「拆完角色之后的按层流水线」。

### 2.3 设计要点


| #   | 设计要点                        | 说明                                        |
| --- | --------------------------- | ----------------------------------------- |
| 1   | **按角色划分**                   | 两侧各持有全部层的不同部分，模块互不重叠                      |
| 2   | **历史缓存只留在注意力侧**             | 加工侧完全没有 KV；方案三下按层驻留在各层卡组                  |
| 3   | **只搬中间数据，不搬中间状态**（不拆层时）     | 残差留在注意力侧省带宽；**方案三按层拆开后此约定要改**（见 4.4）      |
| 4   | **按需裁剪模块**                  | 每侧只加载自己需要的部分                              |
| 5   | **接线错误立刻报错**                | 裁掉的模块被误调用立即失败，不做静默降级                      |
| 6   | **数据格式提前约好**                | 交换的字段、形状、精度启动时固定                           |

### 2.4 做到什么算成功

1. 加工侧**完全没有** KV 和注意力模块 → 显存收益最大化。
2. 两侧模块**互不重叠** → 独立部署、扩容、升级。
3. 代价：每层要过 seam（注意力→加工、加工→注意力各一趟）——这是方案二、三处理的对象。

---

## 三、方案二 · 队列 + 通行证：把节奏管起来

### 3.1 解决什么问题

1. **速度不匹配没有刹车**：没有额度控制，要么积压爆炸、要么空转。
2. **在飞状态不透明**：调度以为发出去了，其实堵在发送口，容量决策建立在错数字上。
3. **活没地方等**：等上游数据和等资源额度是两种「等」，没有等待区就分不清、也防不住饿死。
4. **合批需要明确的就绪集合**，不能靠猜超时。
5. **共用加工池里只有同层能合并，而层号可能是混的**（见 3.3）。

### 3.2 注意力侧：等待区与在跑区

每一层一对「等待区 / 在跑区」，用额度控制放行：

```
                 领到名额              真正上路              算完
   等待区 ─────────────────► 预留中 ─────────────► 在跑区 ──────────► 下一层等待区
   「已到本层；             「已占位，            「真的在路上」          │
     等依赖 / 额度」           还没上路」                                │
      ▲                        │                                      ▼
      └──── 名额没拿到，原样退回 ──┘                    最后一层算完 → 采样
```

**放行条件分两级，实现时必须分开判断**，否则会「互相等待」或「悄悄超发」：


| 判断       | 内容                                                 |
| -------- | -------------------------------------------------- |
| **依赖就绪** | 算本层的输入到了没有。层 L：层 L-1 加工结果已回；**层 0：输入 / embedding 就绪，没有更上游的加工** |
| **资源额度** | 有没有空位。依赖就绪后才去争「在飞名额」，且**必须配防饿死规则**                 |

**名额要「真上路了」才算占用**：先逻辑占位，活真被推出去才标成在跑，
推不出去就**原样退回等待区**（不丢、不乱序）。这样「以为在飞的数量」与「真实在飞的数量」永不错位。

### 3.3 加工侧：按层分桶的队列

共用加工池同时持有很多层的权重，于是有两个后果：

1. **只有同一层的活能合并成一次计算**——不同层权重不同，合在一起算没有意义。
2. **层号会不会混，取决于上游形态**：
   - **单路注意力按波前逐层推进**：同一时刻送来的活本就同层 → 分桶是恒等操作，
     **也没有额外可合的东西**（那一批本身就是合并结果）。
   - **共用池同时服务多路来源**（多工人 / 多 owner / 刻意错层）：层号才真的混 → 不分桶会算错。

所以共用池形态下，队列**必须具备按层分桶的结构**（合并键 = 层号）——这是**正确性**要求。
但**等待凑批是策略，收益是另一回事**：单路无可再合，多路又凑不到，空等只加延迟。

```
   到达的活（多路来源时层号会混）
        │
        ▼
   第一级 · 按层分桶（合并键 = 层号）
     层 L 桶 [活 活 活] ──► 一次调用该层专家网络
     层 L+1 桶 [活]
     层 L+2 桶 [活 活]     桶内可合并，跨桶永不合并
        │
        ▼
   第二级 · 选哪个桶先算：先到 / 最深 / 等最久——**必须防饿死**
```

**两个关键决定**：

- **合并等待窗口默认关闭**。等待直接加在关键路径上，而一条请求要走完所有层：
  每层多等 0.3 ms × 78 层，在 TPOT 1 ms 的目标下完全不可接受。默认来一个算一个，
  只在有实测收益时才开小窗口。
- **选桶必须防饿死**：给长时间没被选中的桶加权，否则突发时某些层会被长期跳过。

### 3.4 设计要点


| #   | 设计要点              | 说明                                     |
| --- | ----------------- | -------------------------------------- |
| 1   | **按层设等待区**        | 不能跨层合批：权重不同，硬合会算错也不会更快                 |
| 2   | **等待区不占额度，在跑区占** | 积压的活只排队；上路的活占名额，算完立刻推进到下一层             |
| 3   | **依赖就绪与在飞名额分开**   | 依赖满足后再争额度；层 0 没有上游加工                    |
| 4   | **名额真上路才算占用**     | 调度视角的在飞数与真实在飞数永不错位                     |
| 5   | **三类独立容量上限**      | 全局在飞 / 单层在飞 / token 预算，三个旋钮分开调          |
| 6   | **单层上限不可省**       | 否则某一层垄断全部名额，其他层长期排不上（公平性，不是为了刻意错层）      |
| 7   | **汇总先收先完成的**      | 不死等队首，避免一个慢活堵住整条波前                     |
| 8   | **防饿死（层与桶都要）**    | 给长时间没被选中的层 / 桶加权                       |
| 9   | **通道数与在飞上限对齐**    | 免得「调度以为发出去了、其实堵在门口」                    |
| 10  | **加工侧按层分桶（共用池）**  | 分桶是正确性结构；合并窗口默认关闭；方案三落地后变静态路由（见 4.3）    |

### 3.5 这个方案的真实定位（重要）

**它管节奏、背压、让活有地方等，不提升吞吐。**

「让好几批活同时停在不同层」我们已完整实现并实测过——**仍是一套注意力算力串行服务各层**，
结果是**更慢**：调度与传送更碎、吞吐不变、单条延迟明显变差。
加工侧的分桶队列同理：它让同层合并在结构上成为可能，但实测打开没有收益，**默认关**。

所以方案二是**支撑件**；吞吐主线交给方案三，不靠多开注意力进程。

---

## 四、方案三 · 按层拆分：把算力铺到每一层

### 4.1 解决什么问题

在 §1.3 的目标下，现在的结构是「同一套注意力算力负责全部层」，于是还剩三个问题：

**一、进不了跨层流水线。** 层 L 在算时，层 L+1 的卡组并不存在，
所以不可能「甲在层 5 算、乙同时在层 0 算」。请求之间可以 CB，但那是**同层合批**，不是跨层流水线。

```
   现在（一套算力管所有层）        想要（每层独立算力）

   甲: 层0→层1→…→层N              甲: 层0→层1→层2→…→层N
   乙: 层0→层1→…                  乙:      层0→层1→…
   ↑ 可与甲同层合批，              丙:           层0→…
     但不能甲算层5时乙算层0         ↑ 每层都在干活，只是干的批次不同
```

**二、共用池里「靠等待凑同层」两头都不划算。**
单路来的本就同层、无可再合；为重叠而让多路错层，同层又凑不到（见 3.3）。
每层有自己的加工单元后，送到这一层的活按定义就是同层，**不需要运行时去凑**。

**三、抖动会占住整套算力。** 一套算力顺序跑完所有层时，任一层抖动会占住它，后面每层都得等。
按层拆开后各层有独立算力 + 层间队列，慢的一层主要在自己队列里积压，**不抢其他层的算力**。
（但这**不能突破「最慢的一层决定整体吞吐」**，只是让抖动不累积、瓶颈可单独加卡。）

### 4.2 怎么解决

**每一层有自己的注意力算力 + 自己的加工单元。** 层内用**多卡分片（TP）+ CB** 吃满并发，
**不默认开多个注意力进程副本**；本层 KV 驻留本层卡组（仍不进加工侧）；
末端的输出 / 采样仍归注意力侧（或独立末端），**不放到 LPU 上**。

```
   每层一组：注意力卡组（TP + CB，本层 KV） + 加工单元（如 LPU）

   层0注意力 ──► 层0加工 ──► 层1注意力 ──► 层1加工 ──► … ──► 层N加工 ──► 输出/采样
      │             │            │                                    （注意力侧末端）
     KV0        残差随路         KV1 …

   · 加工结果投给「下一层注意力」，不是加工连加工
   · 每一级可同时处理不同批次 ← 这就是流水线
   · 每级内部用 CB 把当前在飞的活合成一批算完
```

### 4.3 最大的价值：同层合并从运行时问题变成部署时拓扑

去哪个加工单元由**层号直接决定**（静态路由），到达的活天然同层。
方案二那个「按层分桶 + 等待凑批」不再需要——分桶退化为路由的副产品。

### 4.4 代价：两笔必须付的账

**第一笔：残差必须跟着数据走**（修订方案一第 3 条）。
不拆层时残差留在注意力侧省带宽；按层拆开后，层 L 的注意力算完要把结果**连同残差**交给
层 L 的加工单元，再交给层 L+1 的注意力。相对现状，**每趟传输量大致翻倍**
（现有「多层合并」实现已做过，机制有，要从例外变常态）。

> 在 §1.3 的量级下，多搬一份残差仍远小于每层 KV 访存：
> **这是相对现状的代价，不是 5M TPS 的一阶瓶颈**（一阶瓶颈仍是 KV 带宽）。

**第二笔：部署规模按层放大。** 每层一组注意力卡 + 一个加工单元，单机放不下，
**本质上只适合多机 / 多封装部署**——是部署形态升级，不是改配置。

> 层内再开注意力进程副本会抬高主机侧固定开销与权重读取，**不作为默认手段**；
> 层内并行优先用多卡分片。

### 4.5 设计要点


| #   | 设计要点            | 说明                                 |
| --- | --------------- | ---------------------------------- |
| 1   | **按层建组**        | 每层一组注意力卡 + 一个加工单元，各自独立             |
| 2   | **层号静态路由**      | 去哪个加工单元由层号决定，不再动态凑批                |
| 3   | **层内 CB**       | 本层有多少在飞就算多少，不设门槛空等                 |
| 4   | **层内用卡分片，不用进程副本** | 约每层 20 张卡做 TP（§1.3）                |
| 5   | **加工结果投给下一层**   | 层 L 加工完 → 层 L+1 注意力，形成流水线          |
| 6   | **残差随数据一起走**    | 每趟要多带一份                            |
| 7   | **组包与计算重叠**     | 组包叠进本层 attention 计算，单组卡 + CB 即可藏住  |
| 8   | **按最慢的一级限速**    | 吞吐由最慢层决定；瓶颈层加卡或加强加工单元              |
| 9   | **跨机 / 跨封装部署**  | 层数决定规模，要配套调度                       |

### 4.6 做到什么算成功

1. **不同批次真的停在不同层**（流水线转起来），而不是批次排队。
2. **加工侧不再需要等着凑同层**——同层合并成为路由副产品。
3. 吞吐由**最慢的一层**决定，且能通过给该层加卡 / 加强加工压低。
4. **延迟口径要提前对齐**：拆层本身不增加串行段数（每层仍是「注意力 + 传送 + 加工 + 传送」），
   但三处会让单条延迟变差，且都是预期代价——
   每趟数据变大（残差，低负载下也慢一点）、跨进程交接点变多（p99 变差）、
   **吞吐跑得越满排队越长**（用延迟换吞吐，高负载主因）。
   **判断标准是吞吐**，低负载下不应出现额外排队恶化。

### 4.7 与「不拆层」现状的对比


|           | 现状（不拆层）           | 方案三（拆到层）                         |
| --------- | ----------------- | -------------------------------- |
| 注意力       | 一套算力管**全部层**      | 每层独立卡组，只管本层                      |
| 加工侧       | 共用池，按层号分桶         | 每层独立单元                           |
| 同层合并      | **靠运行时凑，实测无收益**   | **天然成立**（路由副产品）                  |
| 跨批流水线     | 做不到               | **能做到**                          |
| 单条延迟（链深度） | N 层 × 2 段         | **一样**；实际变差来自排队与每趟变大             |
| 层状态 / KV  | 残差留注意力侧；KV 在注意力工人 | 残差随路（每趟约翻倍）；**KV 按层驻留各层卡组**      |
| 输出 / 采样   | 注意力侧末端            | **仍归注意力侧（或独立末端），不在 LPU**         |
| 层内并行      | —                 | **多卡分片 + CB**；不做多注意力进程副本         |
| 部署        | 现有形态              | **多机 / 多封装形态**                   |


**结论**：方案三解决流水线与同层合并，代价是残差带宽与部署规模两笔账；
层内用卡分片 + CB，不必同卡多开注意力进程。

---

## 五、分几步做

- **第 0 步 · 先关掉已知风险**：当前切分路径存在跨序列数据串台隐患，尚未完全定位。
- **第 1 步 · 方案一**：已完成，保持。
- **第 2 步 · 方案二重新定位为纯背压机制**：每趟带足数据不切碎；把「依赖就绪」和「资源额度」
  两个判断拆开；补齐防饿死；加工侧保留按层分桶结构，合并等待窗口保持关闭。
- **第 3 步 · 吃满单路 CB**（几乎零新代码）：不设门槛空等凑批、组包与计算双缓冲重叠、
  提高每趟带的数据量。
- **第 4 步 · 方案三**（目标形态，约束见 §1.3）：按层建组 → 层号静态路由 →
  层内 ~20 卡分片 + CB + 每层 LPU；残差随数据走同步改造；**判断标准是吞吐**；
  不做「同卡多注意力进程」默认路径。

**验证用的实验矩阵**：


| 阶段     | 实验              | 看什么                  |
| ------ | --------------- | -------------------- |
| 方案二    | 背压 / 防饿死        | 节奏正确、不饿死             |
| 吃满 CB  | 加大在飞 / 双缓冲      | 组包延迟是否被计算藏住          |
| 方案三    | 单层端到端           | 一层卡组 + 一层加工单元打通      |
| 方案三    | 多层短流水线（2～4 层）   | 不同批次能否真停在不同层         |
| 方案三    | 流水线跑满           | 吞吐是否由最慢层决定；瓶颈层加卡是否生效 |

---

## 六、风险


| #   | 风险                       | 影响                    | 应对                              |
| --- | ------------------------ | --------------------- | ------------------------------- |
| 1   | 跨序列数据串台隐患                | 正确性                   | 列为第 0 步前置                       |
| 2   | **方案三：传输量翻倍（残差随路）**      | seam 变重；§1.3 下通常仍次于 KV | 复用「多层合并」机制；监控 seam；必要时降精度        |
| 3   | **方案三：部署规模按层放大**         | 单机放不下                 | 明确是多机 / 多封装形态，配套调度              |
| 4   | **方案三：高负载下单条延迟变差**       | 容易被误判为回退              | 提前对齐口径：看吞吐；低负载看是否无额外排队恶化        |
| 5   | **方案三：被最慢的一层限速**         | 一层慢全局慢                | 按层独立加卡 / 加强加工；监控每级耗时            |
| 6   | 空等凑大 batch 伤 TPOT（约 1 ms） | 延迟变差、卡空转              | CB 不设门槛；组包双缓冲                   |
| 7   | 切 microbatch / 同卡多注意力副本  | 权重更难摊、带宽争抢            | **不做默认路径**；层内并行用多卡分片            |
| 8   | KV 全量 all-gather 式切分     | 通信贵过本地 HBM 收益         | 优先按头 TP / KV 复制                 |
| 9   | 每层 LPU 算力配不齐             | 流水线被加工侧限速             | 按层独立配 LPU；监控最慢层                 |

---

## 附录 · 术语与工程对照


| 正文说法              | 代码里的名字                                                   |
| ----------------- | -------------------------------------------------------- |
| 拆开 / 角色切分         | `AfdMode{attn, ffn}`、`module_stubs.py`                   |
| 中间数据契约            | `protocol.py` 的 A2F / F2A 载荷                             |
| 预注册的数据交换区         | `buffers.py` 的缓冲池                                        |
| 等待区 / 在跑区         | 逐层的 ready / running 队列（`token_queue.py`）                 |
| 加工侧按层分桶的队列        | `PerLayerBatchQueue`（默认关闭）；`FFN_GATHER_US` 合并窗口          |
| 合并键 = 层号          | payload 的 `layer_id`                                     |
| 通行证 / 额度          | credit 门控（`farm/attn_farm.py`）                           |
| 先占位、真上路才算         | `reserve` / `commit` / `rollback`（`farm/scheduler.py`）    |
| 三类容量上限            | 全局在飞上限 / 单层在飞上限 / token 预算                               |
| 汇总先收先完成的          | completion-first drain                                   |
| 一趟传送              | hop                                                      |
| 每批活的所有者           | owner / context（`num_contexts`）                          |
| 设定上限              | `COALESCE_K`、`B_STEP`                                    |
| 多工人池化（非默认路径）      | `pool/`：拓扑、路由、额度、多链路                                     |
| 序列粘性派活（非默认路径）     | 按 `req_pool_idx` 路由                                      |
| **方案三：按层建组**      | 每层独立的端点矩阵（`pool/topology.py` 需按层扩展）                      |
| **方案三：层号静态路由**    | 用 `layer_id` 直接选目标，替代动态分桶                                |
| **方案三：残差随数据走**    | layer-merge 的 `residual` / `positions` 槽位（`protocol.py`） |
| **方案三：加工结果投给下一层** | 现有 F2A 回本层 → 改为回下一层注意力                                   |

---

*本文讲设计与方向。实测数据见* `progress.md`*；现行接口契约见* `AFD_DESIGN.md`*。*
